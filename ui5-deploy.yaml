# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: zbarcodescan
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: WCD300
          url: http://sap-wcd:4400
          client: '300'
        app:
          name: ZGXXA_UI_AISSCN
          description: Barcode Scan v1.1
          package: ZG00-ENH-FIN
          transport: WCDK9F22ML
        exclude:
          - /test/
